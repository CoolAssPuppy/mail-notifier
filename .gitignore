.DS_Store
*.mov
dsa_priv.pem
build
DerivedData
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
#*.xcworkspace
!default.xcworkspace
xcuserdata
profile
*.moved-aside
Pods
*.xccheckout

# Secrets
Secrets.xcconfig
# scripts/pull-polar-secrets.sh writes this backup on every run, and it carries
# the Google and Outlook credentials along with the Polar values.
Secrets.xcconfig.bak
.env
vendors/gyb.pyc

# Release artifacts (dist/appcast.xml is the canonical record and is kept)
dist/*.dmg
dist/*.sparkle.txt
dist/*.xcarchive/
dist/export-*/
dist/debug/

# Wrangler (Cloudflare R2 uploads) local cache
.wrangler/
default.profraw
*.profraw

# Local sandbox-test state snapshots
.test-state/
